# Firmware Scaffold

This folder was created by Renode Workbench for one session only.

Session root: `sessions/local-session`

What you still need to provide:

- Your build logic in `firmware/build.ps1`
- Your source files
- Your linker script or project files
- Your Renode script such as `renode/platform.resc` if your workflow needs one

Expected default build output after you customize the script:

- `build/firmware.elf`
